import 'package:flutter/material.dart';

class StorgeScreen extends StatelessWidget {
  const StorgeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Storge Screen'),
      ),
    );
  }
}
